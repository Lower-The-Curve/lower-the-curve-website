# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (keep the example committed)
.env
.env*.local

# vercel / deployment (undecided for later, but ignore by default)
.vercel

# typescript build info (project is JS, but ignore just in case)
*.tsbuildinfo
